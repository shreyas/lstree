.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install lstree

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv lstree
    $ pip install lstree
